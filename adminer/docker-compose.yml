services:
  adminer:
    image: adminer:latest
    container_name: adminer
    restart: always
    ports:
      - "8001:8080"
    environment:
      ADMINER_DEFAULT_SERVER: 192.168.68.8
      ADMINER_DESIGN: 'dracula'
